# writing-pr-description: what the rules rest on

Not loaded at invocation. Maintainer's record.

Voice rules follow `writing-tone` and `writing-commit-message`, both measured
2026-08-28 against this user's own corpora. See those skills' `PROVENANCE.md`.

## What was removed from the lifted version

The previous version was derived from a different author's personal-OSS corpus
of 295 PRs and carried their infrastructure:

- `linear.app` issue URLs. This project's tracker is beads (`den-hoag-xxxx`);
  GitHub issues are used on public repositories.
- uppercase `PROJECT-NNN` ticket ids in the title, measured at 0.0% here.
- a `gh-stack` skill, which is not installed in this configuration.

## The informativeness rule is normative, not measured

The measured habit in this project's history is terser and thinner than it
should be. Match the voice, clear the bar. Owner ruling 2026-08-28: a PR
description written with agent context should carry the reasoning a hand-written
one would omit.
