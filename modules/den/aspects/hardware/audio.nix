{ den, ... }:
{
  # Alias: the audio feature is fully covered by den.aspects.pipewire
  # This aspect exists for discoverability when migrating from features.audio
  den.aspects.audio = den.aspects.pipewire;
}
