apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: json64-dev-issuer
spec:
  acme:
    privateKeySecretRef:
      name: json64-dev-issuer-account-key
    server: https://acme-v02.api.letsencrypt.org./directory
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef:
              key: cloudflare-api-token
              name: json64-dev-secret
